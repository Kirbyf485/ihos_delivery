# NetSuite Objects

Place exported SuiteCloud/SDF object XML here if this integration is later managed with SDF.

For Phase 1, the RESTlet can be created manually in NetSuite using the deployment instructions in `docs/deployment.md`.
